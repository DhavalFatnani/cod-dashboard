# Cursor Rules for Monorepo

lint = "npm run lint --workspaces=false || true"
format = "npm run format --workspaces=false || true"
test = "npm test --workspaces=false || true"

[ci]
node = "20.x"

[conventional_commits]
scopes = ["backend","frontend","migrations","infra","docs","services"]
